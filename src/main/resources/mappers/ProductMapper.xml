<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.fdzc.shop.dao.ProductMapper">
    <sql id="Base Column List">
        id,category_id,product_name,description,detail,
          price,stock_count,create_time,update_time
    </sql>
    <insert id="insert" parameterType="Product">
        insert into product
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id!=null">
                id
            </if>
            <if test="categoryId!=null">
                category_id,
            </if>
            <if test="productName!=null">
                product_name,
            </if>
            <if test="description!=null">
                description,
            </if>
            <if test="detail!=null">
                detail,
            </if>
            <if test="price!=null">
                price,
            </if>
            <if test="stockCount!=null">
                stock_count,
            </if>
            <if test="createTime!=null">
                create_time,
            </if>
            <if test="updateTime!=null">
                update_time,
            </if>
        </trim>
        <trim prefix="values (" suffix= ")" suffixOverrides=",">
            <if test="id!=null">
                #{id},
            </if>
            <if test="categoryId!=null">
                #{categoryId},
            </if>
            <if test="productName!=null">
                #{productName},
            </if>
            <if test="description!=null">
                #{description},
            </if>
            <if test="detail!=null">
                #{detail},
            </if>
            <if test="price!=null">
                #{price},
            </if>
            <if test="stockCount!=null">
                #{stockCount},
            </if>
            <if test="createTime!=null">
                #{createTime},
            </if>
            <if test="updateTime!=null">
                #{updateTime},
            </if>
        </trim>
    </insert>
    <select id="filter" parameterType="Product" resultType="Product">
        SELECT
        <include refid="Base Column List"/>
        from product
        <where>
            <if test="productName!=null and productName!=''">
                and product_name like COUNT('%',#{productName},'%',)
            </if>
        <if test="categoryId!=null and categoryId!=''">
            and categoryId like COUNT('%',#{categoryId},'%',)
        </if>
        <if test="description!=null and description!=''">
            and description like COUNT('%',#{description},'%',)
        </if>
        <if test="detail!=null and detail!=''">
            and detail like COUNT('%',#{detail},'%',)
        </if>

        </where>
        order by update_time desc
    </select>
    <select id="findById" parameterType="String" resultType="Product">
        SELECT
        <include refid="Base Column List"/>
        from product
        where id=#{id}
    </select>
    <update id="update" parameterType="Product">
        update product
        <set>
            <if test="categoryId!=null">
                #{categoryId},
            </if>
            <if test="productName!=null">
                #{productName},
            </if>
            <if test="description!=null">
                #{description},
            </if>
            <if test="detail!=null">
                #{detail},
            </if>
            <if test="price!=null">
                #{price},
            </if>
            <if test="stockCount!=null">
                #{stockCount},
            </if>
            <if test="createTime!=null">
                #{createTime},
            </if>
            <if test="updateTime!=null">
                #{updateTime},
            </if>
        </set>
        where id=#{id}
    </update>
    <delete id="delete" parameterType="String">
        delete from product
        where id=#{id}
    </delete>
</mapper>